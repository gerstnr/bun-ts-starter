# Copy this file to .env.local and fill in values.
# .env.local is gitignored and auto-loaded by direnv.

# Context7 API key (optional; unauthenticated calls are rate-limited)
# Get yours at https://context7.com/dashboard
CONTEXT7_API_KEY=
